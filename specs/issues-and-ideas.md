- Changing between graph modes is pretty common.  Add a toggle on the action panel.

- The mini-map looks a bit plain.  Spice it up a bit.

- The settings view looks misaligned because the time axis section is indented.  Put all the remaining settings in a 'General' section and align the time axis settings with the rest of the settings.